# Plasma Effect

Minimal plasma effect renderer using Pygame and NumPy.

## Run

```bash
pdm add pygame
pdm run python plasma_effect.py
```

Optional recording:

```bash
pdm add opencv-python
pdm run python plasma_effect.py --record --record-duration 20 --palette lava --phase-preset spiral
```
